var cv = <%=raw @cv.to_json %>;
cv1 = JSON.stringify(cv);
var projects = <%=raw @cv.projects.to_json %>;
// $('#myModal').modal('show'); 

$(function() {
  return $("form.new_post").on("ajax:success", function(event, data, status, xhr) {
    debugger
  });
});

$('#myModal').on('show.bs.modal', function (event) {
  debugger
  var modal = $(this)
  modal.find('.name h1').text(cv.name)
  modal.find('.profile p').text(cv.profile)
  modal.find('#array').text(cv1)
})
$('#myModal').modal('show'); 

